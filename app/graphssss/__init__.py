# encoding: utf-8

settings=dict(
        #Seconds to fetch from log
        TIME_TO_FETCH=60,
        #Should I record my own cron/service requests ?
        RECORD_GRAPHSSSS_REQ=False,
        #My resource name (to skip in case RECORD_GRAPHSSSS_REQ=False
        RESOURCE='/graphssss/',
        )
